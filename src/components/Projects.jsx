import { ArrowDownToLine, ArrowUpRight, Check } from 'lucide-react'
import { projects } from '../data/portfolioData'
import { Section } from './common'

function CaseStudy() { return <details className="case-study"><summary>Explore system architecture <ArrowDownToLine size={16} /></summary><div className="case-content"><div><h3>Problem</h3><p>Support complex testing workflows in a modular, maintainable platform.</p><h3>My contribution</h3><p>Frontend features, API integration, backend services and automation workflows within a microservices environment.</p></div><div className="service-flow"><b>React Frontend</b><span>↓ API Gateway ↓</span><b>Authentication · Extraction · Enrichment · Test Generation · Execution · Reporting</b><span>↓</span><b>PostgreSQL · Redis · Artifact Storage</b></div></div></details> }

export default function Projects() { const featured = projects[0]; return <Section id="projects" kicker="04 / SELECTED WORK" title="Systems with depth."><article className="featured"><div><p className="project-type">{featured.type}</p><h3>{featured.name}</h3><p>{featured.description}</p><div className="tags">{featured.technologies.map(item => <span key={item}>{item}</span>)}</div>{featured.github && <a className="text-link" href={featured.github}>View GitHub <ArrowUpRight size={15} /></a>}</div><div className="feature-list">{featured.features.map(feature => <p key={feature}><Check size={15} />{feature}</p>)}</div><CaseStudy /></article><div className="project-grid">{projects.slice(1).map(project => <article className="project-card" key={project.name}><p className="project-type">{project.type}</p><h3>{project.name}</h3><p>{project.description}</p><div className="tags">{project.technologies.map(item => <span key={item}>{item}</span>)}</div></article>)}</div></Section> }
