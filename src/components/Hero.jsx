import { motion, useReducedMotion } from 'framer-motion'
import { ArrowUpRight, Braces, Code2, Terminal } from 'lucide-react'
import { profile } from '../data/portfolioData'
import { ResumeButton, SocialLinks, scrollToSection } from './common'

export default function Hero() { const reduce = useReducedMotion(); return <section id="home" className="hero"><div className="orb one" /><div className="orb two" /><motion.div initial={reduce ? false : { opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .6 }} className="hero-copy"><p className="eyebrow">Hi, I’m</p><h1>Manoj <em>Kumar R</em></h1><h2>{profile.role}</h2><p className="lead">{profile.intro}</p><p className="muted">Software engineer experienced in React, JavaScript, Python, FastAPI, REST APIs, databases, Docker and microservice-based application development.</p><div className="actions"><button className="button" onClick={() => scrollToSection('projects')}>View my work <ArrowUpRight size={16} /></button><button className="button ghost" onClick={() => scrollToSection('contact')}>Contact me</button><ResumeButton /></div><SocialLinks /></motion.div><motion.div initial={reduce ? false : { opacity: 0, scale: .95 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: .15 }} className="terminal"><div className="terminal-top"><i /><i /><i /><span>system-architecture.ts</span></div><code><span>const</span> engineer = {'{'}<br />&nbsp;name: <b>'Manoj Kumar R'</b>,<br />&nbsp;focus: [<b>'React'</b>, <b>'FastAPI'</b>, <b>'AI Automation'</b>],<br />&nbsp;build: () =&gt; <b>'reliable systems'</b><br />{'}'};</code><div className="architecture"><div><Braces /> React client</div><span>↓</span><div><Terminal /> API gateway</div><span>↓</span><div><Code2 /> Microservices</div></div></motion.div></section> }
